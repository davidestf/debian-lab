---
- hosts: 127.0.0.1
  connection: local

  tasks:
      - name: Add packages on FreeBSD
        pkgng:
            name: bind-tools,curl,git,ca_root_nss,mc,rsync,bash,most,bash-completion,vnstat,htop,wget,curl,nginx
